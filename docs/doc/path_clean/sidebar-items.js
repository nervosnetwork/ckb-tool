initSidebarItems({"fn":[["clean","The core implementation. It performs the following, lexically: 1. Reduce multiple slashes to a single slash. 2. Eliminate `.` path name elements (the current directory). 3. Eliminate `..` path name elements (the parent directory) and the non-`.` non-`..`, element that precedes them. 4. Eliminate `..` elements that begin a rooted path, that is, replace `/..` by `/` at the beginning of a path. 5. Leave intact `..` elements that begin a non-rooted path."]],"trait":[["PathClean","The Clean trait implements a `clean` method. It's recommended you use the provided [`clean`] function."]]});